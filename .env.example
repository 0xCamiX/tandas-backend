# ============================================
# YAKU Backend - Environment Variables
# ============================================
# Copy this file to .env and update with your actual values
# cp .env.example .env

# Node Environment
# Options: development, production, test
NODE_ENV=development

# Server Configuration
PORT=3000

# Database Configuration
# Format: postgresql://user:password@host:port/database
# Local development example:
DATABASE_URL=postgresql://postgres:password@localhost:5432/tandas_dev

# For production (external/RDS):
# DATABASE_URL=postgresql://user:password@your-db-host.com:5432/tandas_prod?sslmode=require

# Authentication Configuration (Better Auth)
# Generate a strong secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-change-this-minimum-32-characters-long

# Backend URL (where this API is hosted)
# Development:
BETTER_AUTH_URL=http://localhost:3000

# Production examples:
# BETTER_AUTH_URL=http://your-ec2-ip:3000
# BETTER_AUTH_URL=https://api.yourdomain.com

# Frontend URL (CORS configuration)
# Development:
NEXT_PUBLIC_URL=http://localhost:3001

# Production examples:
# NEXT_PUBLIC_URL=http://your-frontend-ip
# NEXT_PUBLIC_URL=https://app.yourdomain.com

# ============================================
# Optional: Additional Configuration
# ============================================

# Logging Level (if implemented)
# LOG_LEVEL=info

# Email (Resend)
# https://resend.com
RESEND_API_KEY=your-resend-api-key
RESEND_FROM="YAKU <no-reply@yourdomain.com>"

# Rate Limiting (if implemented)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration (if needed)
# SESSION_SECRET=another-secret-for-sessions

# ============================================
# Development Only (not for production)
# ============================================

# Database Direct Connection (for Prisma Studio)
# DIRECT_URL=${DATABASE_URL}

# ============================================
# Security Notes
# ============================================
# 
# 1. Never commit .env to Git (it's in .gitignore)
# 2. Generate strong, random secrets for production
# 3. Use different values for development and production
# 4. Store production secrets in GitHub Secrets for CI/CD
# 5. Enable SSL for database connections in production
# 
# Generate secure secrets:
#   openssl rand -base64 32
#   openssl rand -hex 32
#
# ============================================